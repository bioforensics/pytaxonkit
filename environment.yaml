name: pytaxonkit
channels:
 - bioconda
 - conda-forge
 - defaults
dependencies:
 - black==22.6.0
 - pandas>=1.0
 - python>=3.6
 - pytest>=5.4
 - pytest-cov>=2.8
 - taxonkit>=0.8,<0.14.1
 - wget>=1.20
