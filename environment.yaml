name: pytaxonkit
channels:
 - bioconda
 - conda-forge
 - defaults
dependencies:
 - pandas>=1.0
 - pycodestyle>=2.5
 - pytest>=5.4
 - pytest-cov>=2.8
 - pytest-xdist>=1.31
 - taxonkit>=0.6.0
 - wget>=1.20
